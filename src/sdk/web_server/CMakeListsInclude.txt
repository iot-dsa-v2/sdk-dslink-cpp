list(APPEND WebServerSourceFiles
        web_server.cc
        http_connection.cc
        listener.cc
        login_manager.cc
        websocket.cc
        http_request.cc
        http_response.cc
        user_session_manager.cc
        user_session.cc
        user_login_manager.cc
    )

foreach (file ${WebServerSourceFiles})
    list(APPEND ProjectSourceFiles ${LIB_WEBSERVER}/${file})
endforeach ()