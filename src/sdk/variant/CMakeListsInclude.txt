list(APPEND VariantSourceFiles
       variant.cc
	   msgpack.cc)

foreach(file ${VariantSourceFiles})
  list(APPEND ProjectSourceFiles ${LIB_VARIANT}/${file})
endforeach()

include_directories(${CMAKE_SOURCE_DIR}/deps/msgpack/include)
