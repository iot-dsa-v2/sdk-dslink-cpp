list(APPEND CoreSourceFiles
        session.cc
        server.cc
		client.cc
        session_manager.cc
		editable_strand.cc
		link_strand.cc
        )

foreach(file ${CoreSourceFiles})
    list(APPEND ProjectSourceFiles ${LIB_CORE}/${file})
endforeach()
